function client($key)
{
    switch (key) {
        case "create":
            
            break;
        case "index":
            
            break;
    
        default:
            break;
    }
}

module.exports = { client };
